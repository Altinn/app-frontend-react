import React from 'react';

interface RepeatingGroupNextType {
  dings: string;
}

export const RepeatingGroupNext: React.FunctionComponent<RepeatingGroupNextType> = ({}) => (
  <div>
    <h1>Here, we shall repeat group</h1>
  </div>
);
